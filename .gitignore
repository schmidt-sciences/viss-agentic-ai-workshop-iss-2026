# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual envs / uv
.venv/
.uv/
uv.lock.bak

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
*.swp

# Marp render output
slides.html
slides.pdf
slides.pptx

# AWS / secrets
.env
.env.*
!.env.example
.aws/

# Artifacts written by the Block 3 research-loop workflow commands
# (/research, /plan, /implement, /validate). Demo run output, not source.
# The curated fallback copies live in blocks/*/demo/expected-artifacts/.
.agents/

# Symlink created at container-create time by .devcontainer/on-create.sh so the
# Block 3 demo folder shares the repo-root .github (prompts/instructions/skills).
# Not committed because git symlinks don't survive checkout on every platform.
/blocks/03-research-loop/demo/.github
